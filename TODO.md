# TradingAgents TODO

本文档记录 TradingAgents 后续迭代方向。目标不是继续堆叠单点 Agent，而是把系统逐步演进为一套可持续运行的投研工作流：能自动规划、自动执行、自动复盘，并最终服务真实研究决策。

## 1. 当前框架定位

当前主链路已经形成：

- `company_context`
- `technical / fundamental / event_news / sector_flow` 并行分析
- `strategy_style` 识别策略范式
- `strategy_decision` 输出交易动作
- `company_report` 输出完整研究报告
- `final_report` 汇总为面向用户和上层 Agent 的统一结论

当前系统本质上是一个结构化的单票投研引擎，已经具备：

- 多维分析拆分
- LangGraph 编排
- 结构化输出
- 完整 trace / artifact / viewer
- 研究会话 `research` 视角

但和成熟投研团队相比，还缺少预测、估值、跟踪、组合、研究治理和自治调度能力。

## 2. 迭代总原则

- 优先补齐研究闭环，而不是盲目增加 Agent 数量
- 优先补齐可复用的数据结构、状态结构和协议，再做 UI 和 Prompt 微调
- 优先让 Agent 输出能服务上层节点，而不是只产出好看的长文本
- 优先做“可追踪、可验证、可回放”的 workflow，再做更强自治
- 多 Agent 的价值应来自职责分工和信息压缩，而不是角色数量

## 3. 主线 A：研究能力补强

### A1. 预测与估值层

- [ ] 新增 `ForecastValuationAgent`
- [ ] 对关键财务科目做结构化预测：收入、毛利率、净利率、资本开支、现金流
- [ ] 输出估值框架：PE / PB / EVEBITDA / DCF / 分部估值中的适用方法
- [ ] 输出目标价区间、隐含收益空间、核心估值假设、敏感性分析
- [ ] 让 `strategy_decision` 明确消费估值层结果，而不是只依赖描述性结论

目标：

- 让系统从“解释公司”升级为“预测公司”
- 让最终建议具备更接近正规研究的可落地估值锚

### A2. 横向比较层

- [ ] 新增 `PeerComparisonAgent`
- [ ] 自动识别同业可比公司、产业链上下游和替代标的
- [ ] 输出相对估值、相对成长、相对景气位置、相对资金偏好
- [ ] 让报告天然包含“为什么选它而不是同业”

目标：

- 补齐正规研究里非常重要的比较视角

### A3. 假设与跟踪层

- [ ] 新增 `HypothesisTrackingAgent`
- [ ] 从各分析结果中抽取核心投资假设
- [ ] 输出每条假设对应的验证指标、更新频率、失效条件、下一次检查时间
- [ ] 将研究从一次性分析升级为持续 coverage

目标：

- 让研究具备“后续跟踪”和“动态修正”能力

## 4. 主线 B：Workflow 能力升级

### B1. 从单次运行升级为持续研究工作流

- [ ] 引入 `coverage` 或 `research_program` 概念
- [ ] 一个 research 不只保存 run 和消息，还保存长期的研究目标、历史结论、关键假设、更新记录
- [ ] 支持定期重跑、事件触发重跑、关键指标异动触发重跑

目标：

- 从“用户问一次，系统答一次”升级为“系统持续维护一个研究对象”

### B2. 引入研究任务分解

- [ ] 在 graph 入口增加 `ResearchPlannerAgent`
- [ ] 根据用户问题自动拆解研究子任务
- [ ] 决定是否需要跑完整图、轻量图、补充事件、补充估值、补充行业比较
- [ ] 让系统支持“问题驱动”的动态 workflow，而不总是固定全量执行

目标：

- 减少不必要计算
- 让 workflow 更接近真实研究团队的任务拆解过程

### B3. 引入质量门控

- [ ] 对关键节点增加质量评估器：结果是否完整、证据是否足够、结论是否冲突
- [ ] 若质量不足，自动回退到补数、补搜、补分析流程
- [ ] 为每个 Agent 输出增加 `confidence`, `evidence_level`, `missing_info`

目标：

- 让 workflow 不只是串行执行，而是具备“检查-返工-收敛”能力

## 5. 主线 C：Multi-Agent 架构升级

### C1. 从并行分析升级为协同分析

- [ ] 保留 `technical / fundamental / event_news / sector_flow` 四条主线
- [ ] 增加共享中间语义层，而不是只在最后汇总
- [ ] 支持跨 Agent 引用：
- 例如事件面发现政策变化后，触发基本面重估
- 例如板块热度变化后，重新约束技术信号解释

目标：

- 让多 Agent 真正协同，而不是多个独立报告的拼接

### C2. 增加反方 / 质疑型 Agent

- [ ] 为关键结论引入 `ChallengeAgent` 或 `RedTeamAgent`
- [ ] 专门寻找证据缺口、逻辑跳跃、过度乐观/悲观判断
- [ ] 将 challenge 输出传入 `strategy_decision` 作为反方约束

目标：

- 提高结论稳健性
- 避免 LLM 输出过于流畅但不够审慎

### C3. 增加研究经理层

- [ ] 新增 `ResearchManagerAgent`
- [ ] 不直接做分析，而负责分配任务、审阅子 Agent 输出、决定是否补做某类分析
- [ ] 输出研究摘要、当前研究状态、剩余不确定性

目标：

- 让整个系统更像一个真实投研团队，而不只是固定 DAG

## 6. 主线 D：模型自我规划与自我执行

### D1. 自主任务规划

- [ ] 让 `ResearchPlannerAgent` 根据用户问题自动决定：
- 是否需要完整分析
- 是否只需要补事件面
- 是否需要二次查询 Tushare / 妙想
- 是否需要补横向比较或估值

目标：

- 模型不只是回答问题，而是先判断应该怎么做研究

### D2. 自主执行与补救

- [ ] 为每个关键节点定义 `missing_info` 与 `retry_policy`
- [ ] 如果某个 Agent 证据不足，则自动触发补充检索或补充上下文构建
- [ ] 将当前失败从“直接失败”升级为“自动补救后再尝试”

目标：

- 提升系统鲁棒性
- 把失败从终点变成 workflow 的一部分

### D3. 自主复盘

- [ ] 新增 `PostRunReviewAgent`
- [ ] 自动评估本次研究有哪些信息缺口、哪些结论过弱、哪些 Agent 输出质量低
- [ ] 输出下一轮 prompt、schema、数据依赖的改进建议

目标：

- 让系统具备“自己帮助自己迭代”的能力

## 7. 主线 E：组合与投资决策升级

### E1. 组合视角

- [ ] 新增 `PortfolioContextAgent`
- [ ] 输入当前持仓、行业暴露、风险预算、现金比例
- [ ] 判断单票建议在组合层是否合理
- [ ] 输出“适合买入”与“适合纳入当前组合”之间的区别

### E2. 候选池排序

- [ ] 支持多个研究对象统一比较
- [ ] 输出候选池优先级、替代关系、边际增配价值
- [ ] 支持从单票研究升级为研究池管理

## 8. 主线 F：研究治理与数据治理

### F1. 研究协议标准化

- [ ] 所有 Agent 输出统一 schema 版本
- [ ] 统一 `summary / evidence / risks / triggers / invalidations / confidence / missing_info`
- [ ] viewer、API、artifact 使用统一协议

### F2. 证据分级

- [ ] 对数据源和结论证据做等级区分：
- 公告 / 财报 / 官方数据
- 高可信行业数据库
- 新闻与媒体
- 市场行为与推断
- 在最终决策中显式暴露证据等级

### F3. 研究版本化

- [ ] 记录每次 research 的关键结论变更
- [ ] 记录哪条假设失效导致结论变化
- [ ] 支持对比两次研究的差异

## 9. 推荐优先级

### Phase 1：补齐研究闭环

- [ ] `ForecastValuationAgent`
- [ ] `HypothesisTrackingAgent`
- [ ] 统一 evidence/confidence/missing_info schema
- [ ] `PostRunReviewAgent`

### Phase 2：升级 workflow

- [ ] `ResearchPlannerAgent`
- [ ] 轻量图 / 全量图 / 补充图 三类执行模式
- [ ] 自动返工与质量门控
- [ ] 持续 coverage 机制

### Phase 3：升级为真实 multi-agent 研究系统

- [ ] `ResearchManagerAgent`
- [ ] `ChallengeAgent`
- [ ] `PeerComparisonAgent`
- [ ] 组合上下文与候选池排序

## 10. 成功标准

后续迭代不应只看“回答更长”或“Agent 更多”，而应看以下指标是否提升：

- [ ] 研究结论是否更稳定
- [ ] 关键判断是否有清晰证据链
- [ ] 输出是否能直接服务上层决策
- [ ] 失败是否能自动补救
- [ ] 研究是否可持续跟踪
- [ ] 单票分析是否能自然扩展到组合决策
- [ ] viewer 是否能完整呈现 planning / execution / review 全链路

## 11. 暂不优先做的事

- [ ] 暂不继续堆叠“同质化分析 Agent”
- [ ] 暂不把重点放在更花哨的 UI 展示，而忽视研究协议
- [ ] 暂不直接追求自动交易执行
- [ ] 暂不引入过多规则系统替代 LLM 规划

当前更应该做的是：把研究过程做深，把 workflow 做稳，把 Agent 协作关系做清楚。
